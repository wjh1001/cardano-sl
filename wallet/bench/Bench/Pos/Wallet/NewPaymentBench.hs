module Bench.Pos.Wallet.NewPaymentBench
    ( runNewPaymentBenchmark
    ) where

import           Universum

--import           Criterion.Main (bench, defaultConfig, defaultMainWith, nfIO)
--import           Criterion.Types (Config (..))
--import           Weigh (io, mainWith)

--import           Test.QuickCheck (arbitrary, generate)

-- import           Test.Pos.Util (withDefConfigurations)

----------------------------------------------------------------
-- GetHistory benchmark
----------------------------------------------------------------

-- | Space @getBlocksPage@.
runNewPaymentBenchmark :: IO ()
runNewPaymentBenchmark = return ()
